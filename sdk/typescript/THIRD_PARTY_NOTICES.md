# Third-party notices

The generated sources identify `@hey-api/openapi-ts`, used under the MIT
License. The generator is a development dependency and is pinned in
`package-lock.json`.

TypeScript and the Node.js type declarations are development-only build and
test dependencies. Their exact package versions, integrity hashes, and license
metadata are retained in `package-lock.json` and the installed package metadata.
